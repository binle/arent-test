import { RecordType } from '../../definitions';
// import { axiosClient } from './_axios-client';

export const asyncAppStaticData = async (): Promise<RecordType> => {
  // return await axiosClient.get('/api/app-static');
  return {};
};
